import { Routes } from '@angular/router';
import { VideoMaskerComponent } from './video-masker/video-masker.component';

export const routes: Routes = [{ path: '', component: VideoMaskerComponent }];
